# Database
DB_USER=root
DB_PASSWORD=       # leave empty if no password for XAMPP MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=compliance_db

# JWT
JWT_SECRET=your-very-secret-key-change-this
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30